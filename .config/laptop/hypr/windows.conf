# User window rules that override Omarchy defaults.

# Omarchy tags mpv as floating-window by default; remove that tag and force tiling.
windowrule = tag -floating-window, match:class ^mpv$
windowrule = tile on, match:class ^mpv$
