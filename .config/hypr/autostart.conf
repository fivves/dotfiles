# Extra autostart processes
# exec-once = uwsm app -- my-service

# Sets the cursor to Moga
exec-once = hyprctl setcursor Moga-Cursor 24

# Restart Waybar with an output chosen from the active Hyprland monitor layout.
exec-once = ! omarchy-toggle-enabled waybar-off && uwsm app -- ~/.config/waybar/scripts/launch.sh

# Opens PC Panel, StreamDeck UI, then closes both windows.
exec-once = uwsm app -- PCPanel
exec-once = uwsm app -- streamdeck
exec-once = sh -c 'sleep 3 && ~/.config/hypr/close-autostarts.sh'

# Launches Sunshine on boot
exec-once = uwsm app -- sunshine

# Launches Music Presence on boot + Launch fix
# exec-once = sleep 5 && uwsm app -- musicpresence

# Audient Fix
exec-once = bash -c 'card=$(aplay -l | grep "iD14" | sed "s/:.*$//g" | sed "s/card //"); amixer -c $card sset "Speaker" 100%'
